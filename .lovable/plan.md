No card "Antes" da seção comparativa em `src/pages/LpAnuncio.tsx`, substituir o ícone de balão de chat de cada item por um `X` vermelho (lucide `X` em círculo vermelho claro). No card "Depois", manter/garantir o `Check` verde (já existe em ciano — trocar para verde) em cada item.

Mudança restrita a esses ícones, sem alterar layout, textos ou cores dos cards.